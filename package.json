{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm --workspace @travel-af/shared run build",
    "dev:web": "npm --workspace web run dev"
  },
  "packageManager": "pnpm@9.12.2",
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
